I used Navigator.push with MaterialPageRoute for navigation. I chose this approach because it is simple, clear, and ideal for small-to-medium apps where passing arguments and returning results between screens is straightforward. It also makes the data flow (push → await result → pop with data) easy to understand and manage.
